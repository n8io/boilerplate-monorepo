import { styles as linkStyles } from './link';
import { Palette } from './palette';
import { styles as skipLinkStyles } from './skipLink';

export const Color = {
  ...Palette,
  linkStyles,
  skipLinkStyles,
};
