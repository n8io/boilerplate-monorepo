import { Context } from '../context';

const Provider = {
  AUTH: Context.AUTH.Provider,
  FEATURE_FLAGS: Context.FEATURE_FLAGS.Provider,
  INTERNET_CONNECTIVITY: Context.INTERNET_CONNECTIVITY.Provider,
  MODALITY: Context.MODALITY.Provider,
  THEME_SWITCHER: Context.THEME_SWITCHER.Provider,
};

export { Provider };
