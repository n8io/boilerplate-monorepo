import { Layout } from 'types/layout';

const { COMFORTABLE, COMPACT, DEFAULT } = Layout;

export const Spacing = {
  [COMFORTABLE]: '0.5rem 0.75rem',
  [COMPACT]: '0.125rem 0.5rem',
  [DEFAULT]: '0.5rem 1rem',
};
