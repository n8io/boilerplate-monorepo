import React from 'react';
import { Context } from '../context';
import { Size } from '../size';
import { Type } from '../type';

const Button = props => <x-Button {...props} />;

export { Button, Context, Size, Type };
